#include<stdio.h>

int sum(int a, int n)
{
	int i = 0;
	int add = 0;
	int s = 0;
	for (i = 0; i < n; i++)
	{
		int p = pow(10, i);
		add = add + a * p;
		s = s + add;
	}
		return s;
}
int main()
{
int a=1;
	int n=1;
	scanf("%d %d", &a,&n);
	int r=sum(a, n);
	printf("%d\n", r);
	return 0;
  }
